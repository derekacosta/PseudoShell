myShell README


Notes:
When compiling (without Makefile), if readline does not exist, install
using home-brew, and then you can compile with -lreadline.

Still needs support for:
- Multiple pipes
- History